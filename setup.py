from setuptools import setup

requirements = [
      'matplotlib',
      'numpy',
      'pandas',
      'seaborn']

setup(name='DataSynthesizer',
      version='1.0',
      install_requires=requirements
      )
